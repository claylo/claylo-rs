{
  "name": "@{{ owner }}/{{ project_name }}-linux-arm64",
  "version": "0.1.0",
  "description": "{{ project_description }} (linux-arm64 binary)",
  "license": "{% if license | length > 1 %}({{ license | join(' OR ') }}){% else %}{{ license | first }}{% endif %}",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{ owner }}/{{ project_name }}"
  },
  "os": ["linux"],
  "cpu": ["arm64"],
  "files": ["bin/"]
}
